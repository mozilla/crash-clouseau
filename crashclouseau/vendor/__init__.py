"""Vendored, import-rewritten subset of mozilla/bugbug's hackbot substrate.

See README.md for the source commit and the exact keep/drop set. Absolute
imports were rewritten ``hackbot_runtime.*`` -> ``crashclouseau.vendor.hackbot_runtime.*``
and ``agent_tools.*`` -> ``crashclouseau.vendor.agent_tools.*``; nothing else changed.
"""
