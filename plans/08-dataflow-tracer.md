# 08 — Senior #4: Data-flow Tracer

## Objective
For one `(candidate patch, crash frame)` pair, fetch the actual source of the functions along the verified call path (`searchfox-cli define`) plus the candidate's raw unified diff, and have a mid/strong-tier LLM reason about whether the change can **free / mutate / null / overrun** the value the crash site dereferences — the data-flow inference searchfox's edge graph cannot express. It emits a structured, fully-cited `DataFlowHypothesis` (or a "no plausible data flow" verdict) that the Skeptic re-checks and the Principal consumes. This is the read-and-reason step that replaces the maintainer's brain.

> **Substrate (adopt hackbot, see #02):** this role is a hackbot `claude-agent-sdk` `AgentDefinition` (`model="sonnet"` per Phase-0/#02 tiering, `effort` set at the options level; `opus` as the config-selectable escalation) run under the #02 principal loop and spawned via the built-in `Task` tool. It pulls path bodies + diff lines through in-process MCP tools (`build_sdk_server` over `agent_tools` `@tool` wrappers of the #01 searchfox `define` client and the #14 patch-extraction helper), **not** a bespoke `llm_call("data_flow_tracer",...)`. Its output is the trailing ` ```json ` `DataFlowHypothesis` hand-off block, Pydantic-validated per #03 (coerce to `verdict="insufficient"` when a claim lacks its body/diff citation) — there is **no** `messages.parse`/`output_format`.

## Scope
**In scope**
- A `trace_dataflow(dossier, candidate, path)` entry point that, per `(patch, frame)` pair, gathers function bodies, attaches them to the diff, and runs as the `data-flow-tracer` `AgentDefinition` under the #02 SDK loop, emitting a trailing ` ```json ` `DataFlowHypothesis` fragment validated per #03 (coerced to `verdict="insufficient"` when a claim is uncited).
- Building the prompt context: pull bodies for each hop on the call path via `searchfox-cli define`; fetch the candidate raw diff (`RawRevision.get_revision(channel, node)`); map the candidate's changed line numbers (`Changeset.added_lines/deleted_lines/touched_lines`) into the diff hunks so cites are exact.
- Defining the `DataFlowHypothesis` Pydantic model and writing it into the dossier (`candidate.dataflow`).
- Identifying the *mechanism class* (UAF / null-deref / mutate-after-pass / OOB) and the *target value* (the object/field the crash site touches), each with a source-line citation (file, function symbol, line, searchfox permalink).

**Out of scope (owned elsewhere)**
- The searchfox-cli Python adapter (subprocess wrapper, permalink/symbol-id capture) — owned by the Call-graph Explorer sub-plan; this unit *calls* `searchfox.define(...)`.
- The `AgentDefinition`/`ClaudeAgentOptions` assembly, per-role model tiering, MCP-server wiring, and the trailing-` ```json ` extraction — owned by #02 (its `run_crash_triage`/`ClaudeSDKClient` principal loop authors `roles.py` and spawns this role via the built-in `Task` tool); this unit only supplies the `data-flow-tracer` role prompt, its tool allowlist, and the `DataFlowHypothesis` output model.
- The crash brief (mechanism class at the crash site, faulting address, expanded inlines) — produced by the Crash Interpreter; this unit *reads* `dossier.crash_brief`.
- The candidate set, neighborhood, call-path edges, and the "what changed semantically" one-liner — produced by Call-graph Explorer + Patch Scout; this unit reads `candidate.call_path` and `candidate.diff`.
- Independent re-verification of every claimed line/edge — owned by the Skeptic.
- The final verdict / abstain / needinfo draft — owned by the Principal.
- Dossier persistence table and the RQ worker chaining — owned by the worker/persistence sub-plan; this unit is a pure function over a dossier object.

## Externalities

| Name | Kind | Version / Endpoint / Command | Status | Purpose |
|---|---|---|---|---|
| `pydantic` | python-lib | `pydantic>=2` (owned by #01/#03) | NEW | `DataFlowHypothesis` typed model + dossier field validation at hand-off. `pydantic>=2` is owned by #01/#03; this unit only declares/uses models. |
| Claude Sonnet 5 | llm-model | `claude-sonnet-5` — $3/$15 per 1M (1M ctx) | NEW | Default tier for the `data-flow-tracer` role (reasoning-heavier senior). Supports options-level `effort` (`low..max`) and adaptive thinking (`thinking={"type":"adaptive"}`); emits its `DataFlowHypothesis` fragment as a trailing ` ```json ` block validated per #03. Prompt caching is SDK/CLI-managed under the #02 substrate. |
| Claude Opus 4.8 | llm-model | `claude-opus-4-8` — $5/$25 per 1M (1M ctx) | NEW | Optional escalation tier for hard pairs (config-selectable per role). Same request surface as Sonnet 5 (adaptive thinking + effort); prompt caching is SDK/CLI-managed (see #02). |
| #02 agent substrate | internal-module | `crashclouseau/agent/roles.py` (`AgentDefinition`) + `crashclouseau/agent/triage.py` (`run_crash_triage`/`ClaudeSDKClient`), owned by #02 | NEW | This role is the `data-flow-tracer` `AgentDefinition(model=...)` authored in `roles.py`, spawned by the #02 principal loop via the built-in `Task` tool; its per-role tier comes from the `agent.llm.roles` config block, its evidence tools are `build_sdk_server` MCP servers (`mcp__searchfox__*` / `mcp__patch__*`), and its trailing ` ```json ` `DataFlowHypothesis` fragment is validated per #03. |
| searchfox-cli `define` | CLI | `searchfox-cli` (github.com/padenot/searchfox-cli) symbol/definition lookup + full-function source extraction; repo selected via the adapter (`mozilla-central`/`mozilla-beta`/`mozilla-release`/`mozilla-esr*`/`comm-central`). | NEW (external) | Pull the actual body of every function on the call path so the LLM can reason about how the changed code touches the target value. Invoked through the shared searchfox adapter, not shelled out here. (Exact CLI subcommand name/flags for "define" are owned + verified by the Call-graph Explorer adapter, which is the single integration point.) |
| searchfox-cli repo selection | config | repo arg routed through the adapter (`mozilla-central`/`mozilla-beta`/`mozilla-release`/`mozilla-esr*`/`comm-central`) | NEW | Searchfox indexes ~tip of the firefox source (post hg→git migration, `github.com/mozilla-firefox/firefox`); the revision-drift note must be passed to the LLM. |
| searchfox adapter | internal-module | `crashclouseau/agent/searchfox.py` (Call-graph Explorer sub-plan) — `define(symbol, repo)` returning body + permalink + symbol-id | NEW | This unit depends on `define`; it does NOT shell out directly. |
| `crashclouseau.patch.parse` | internal-module | `parse(chgset, channel="nightly", chunk_size=...)` → parsepatch FLAT result (per-file added/deleted/modified line **numbers**, no hunk text) | existing | Already gives per-file changed line numbers used to anchor diff cites; reused, not re-implemented. Note: `patch.parse` filters via `utils.is_interesting_file` and `skip_comments=True`. |
| `parsepatch` | python-lib | `parsepatch>=0.1.3` (in `requirements.txt`) | existing | Underlies `patch.parse`; FLAT mode (line numbers, no hunk text). |
| Patch extraction (`#14`) | internal-module | `crashclouseau/agent/patch_extract.py`: `extract(node, channel)` → hunk text + line cites + `@@` enclosing functions | NEW (foundation) | **Single source of hunk text + diff-line cites** for `_anchor_diff` — this unit no longer re-fetches `RawRevision` itself; it reads the canonical extraction (the same one Patch Scout used). |
| `libmozdata.hgmozilla.RawRevision` | internal-module | `RawRevision.get_revision(channel, node)` fetches the raw unified diff for `node`; `RawRevision.get_url(channel)` returns only the base `…/raw-rev` URL (no node), so it is **not** sufficient on its own to fetch a diff | existing | Source of the actual hunk text (added/removed lines) the Tracer cites; `patch.parse` gives only line numbers. |
| `libmozdata` | python-lib | `libmozdata>=0.2.12` (in `requirements.txt`) | existing | Provides `hgmozilla.RawRevision`; reused. |
| `requests` | python-lib | `requests>=2.34.2` (in `requirements.txt`) | existing | Used transitively by libmozdata / the searchfox adapter; no direct new use here. |
| `crashclouseau.models.Changeset` | internal-module | `added_lines` / `deleted_lines` / `touched_lines` (pg ARRAY of int), `isnew` (Boolean); `find` returns `{filename: [node, ...]}` (`models.py:338`); `get_scores` (`:363`); has an `analyzed` Boolean column gating `get_scores` | existing | Read changed line numbers per file for the candidate node to anchor the hunk cites. (`find` returns node strings keyed by filename — fetch the `Changeset` row by node to read the `*_lines` arrays.) |
| `crashclouseau.models.Node` | internal-module | `node`, `channel`, `bug`, `merge`, `backedout`, `pushdate`, `hgauthor` columns (`models.py:127`) | existing | Candidate identity (node/bug/author) echoed into the hypothesis provenance; `backedout` is a Skeptic concern, read-only here. `hgauthor` is an FK to `hgauthors.id` (not the author string directly). |
| `crashclouseau.models.CrashStack` | internal-module | `filename`, `function`, `line`, `stackpos`, `node`, `uuidid`, `module`, `internal` (`models.py:1176`); `put_frames` (`:1223`); `get_by_uuid` (`:1264`) | existing | The crash-site frame fields (the value/derefs at `frame.line`) that the data-flow target is checked against. |
| `crashclouseau.config` | config | `./config/global.json` (loaded by `config._get_global`); add an `"agent"`/`"llm"` block; new keys `agent.dataflow.{model, effort, max_path_bodies, max_body_chars}` | NEW keys (existing loader) | Model tier + budget knobs for this role. `config.py` has no `get_agent_*` helpers today — add `config.get_dataflow_cfg()` (or reuse the LLM sub-plan's accessor). |
| `crashclouseau.logger.logger` | internal-module | `from .logger import logger` (module exposes a configured root `logger`) | existing | Structured logging of pairs traced / abstained / body-fetch failures. |
| dossier schema | internal-module | `crashclouseau/agent/schema.py` (Dossier-contract sub-plan) — `Dossier`, `Candidate`, `CallEdge`, `Citation`, `crash_brief` | NEW (other sub-plan) | This unit reads `crash_brief`, `candidate.call_path`, `candidate.diff` and writes `candidate.dataflow`. |
| `crashclouseau/agent/` package | internal-module | new package dir; this unit adds `dataflow.py` + the package `__init__.py` | NEW | The `crashclouseau/agent/` directory does not exist yet. The top-level `crashclouseau/__init__.py` is the Flask app module (defines `app`, `db`) and is **not** a re-export surface — exports go in `crashclouseau/agent/__init__.py`. |

## Deliverables
**Create**
- `crashclouseau/agent/dataflow.py`
  - `DataFlowHypothesis` (Pydantic) — fields: `verdict` (`Literal["plausible","implausible","insufficient"]`), `mechanism` (`Literal["uaf","null_deref","mutate_after_pass","oob","double_free","uninitialized","other"]`), `target_value` (str — the object/field the crash site touches), `target_citation` (`Citation`), `summary` (str — the one-paragraph causal sentence), `evidence_lines` (list of `Citation` — the diff lines + body lines that support the claim), `consistency_with_crash` (str — how it matches `crash_brief` mechanism/address), `confidence` (`Literal["low","medium","high"]`), `caveats` (list of str — revision drift, missing edge, indirect dispatch). All objects `additionalProperties:false`, no `minLength`/`maxLength`/`minimum`/`maximum`, no recursion (Claude structured-output JSON-schema limits). `Citation` reused from `schema.py` (per #03).
  - `trace_dataflow(dossier, candidate, path_index=0) -> DataFlowHypothesis` — orchestrates body-gathering + prompt context, runs as the `data-flow-tracer` `AgentDefinition` under the #02 SDK loop, then parses/validates the trailing ` ```json ` `DataFlowHypothesis` fragment per #03, attaches to `candidate.dataflow[path_index]`, returns it.
  - `_gather_path_bodies(call_path, repo) -> list[FunctionBody]` — for each hop function symbol, call `searchfox.define(...)`; cap count/size by config; record permalink+symbol-id per body; tolerate `define` misses (record as a caveat, don't fail).
  - `_anchor_diff(candidate, files) -> list[DiffHunkCite]` — read the candidate's hunk text + line cites from the patch-extraction foundation (`#14`, `agent.patch_extract.extract(node, channel)`) — **no independent `RawRevision` fetch here** — and tag each with `Changeset.added_lines/deleted_lines/touched_lines` to produce `(file, line, "added|deleted|touched", text)` cites.
  - `_build_prompt(crash_brief, candidate, path_bodies, diff_hunks)` -> system + user message blocks (volatile pair last; shared crash brief + diffs as the stable prefix; prompt caching is SDK/CLI-managed per #02 — no manual `cache_control`).
- `tests/agent/test_dataflow.py` — schema-validity, body-gathering fallbacks, diff-anchoring, and a golden case with the SDK (`ClaudeSDKClient`/MCP tools) mocked (UAF: `Release(mObj)` moved before early return).

**Modify**
- `requirements.txt` — add the `pydantic` floor (shared with #01/#03; add only if not already added by an earlier sub-plan). The LLM SDK dep `claude-agent-sdk>=0.2` is owned once by #02 — do **not** add `anthropic`. Keep the file's existing one-floor-per-line style.
- `config/global.json` — add an `agent.dataflow` block (`model`, `effort`, `max_path_bodies`, `max_body_chars`).
- `crashclouseau/agent/__init__.py` — export `trace_dataflow`, `DataFlowHypothesis` (create the `agent` package if this is the first sub-plan to land; otherwise just add exports). Do **not** touch the top-level `crashclouseau/__init__.py` (Flask app module).

## Interfaces
**Reads (dossier in):**
- `dossier.crash_brief` — mechanism class at the crash site, faulting/poison address, `moz_crash_reason`, expanded inlines, the crashing frame (file/function/line). The Tracer's "target value" and "consistency" are checked against this.
- `candidate.node` / `candidate.bug` / `candidate.author` — provenance (from `Node`; note `Node.hgauthor` is an FK id, so the human-readable author string is resolved upstream and surfaced as `candidate.author`).
- `candidate.call_path` — ordered list of `CallEdge`s (each with a searchfox citation) from a stack frame to the changed function; the function symbols to `define`.
- `candidate.diff` — Patch Scout's per-file changed-function summary + line numbers (originating from `patch.parse` / `Changeset.*_lines`).

**Writes (dossier out):**
- `candidate.dataflow[path_index] = DataFlowHypothesis` — the cited hypothesis. A hypothesis whose `evidence_lines` is empty or whose citations lack permalinks/symbol-ids is **invalid** and must be coerced to `verdict="insufficient"` (grounding rule §6/§2).

**Depends on:** patch-extraction foundation (`#14`, hunk text + line cites), Dossier-contract sub-plan (types + `validate_role_fragment`), #02 agent substrate (`roles.py` `AgentDefinition`, `run_crash_triage`/`ClaudeSDKClient`, per-role tiering, SDK-managed caching, trailing-` ```json ` parse), Call-graph Explorer sub-plan (`searchfox.define` adapter + `call_path`), Patch Scout (`candidate.diff`), Crash Interpreter (`crash_brief`).
**Feeds:** Skeptic (re-checks each `evidence_line` against diff/index) and Principal (consumes the surviving hypothesis in the per-candidate dossier).

## Implementation steps
1. Confirm/await the dossier contract: import `Dossier`, `Candidate`, `CallEdge`, `Citation`, and the `crash_brief` shape from `crashclouseau/agent/schema.py` (per #03). If not yet landed, stub a minimal `Citation` (file, symbol_id, permalink, line, text) and a `Candidate` protocol to develop against; replace with the real import on merge.
2. Define `DataFlowHypothesis` per the schema-limit rules (no `minLength`/`maxLength`/`minimum`/`maximum`, no recursion; `additionalProperties:false` on every object; `Literal` enums). Keep it flat — citations are a list of the shared `Citation` type. (The Agent SDK/CLI strips unsupported constraints, but author the model without them to keep the schema honest.)
3. Implement `_anchor_diff`: fetch the raw unified diff for `candidate.node` via `RawRevision.get_revision(channel, candidate.node)` (NOT `get_url`, which returns only the base `/raw-rev` URL), and align it with `Changeset.added_lines/deleted_lines/touched_lines` (read from the `Changeset` row for the candidate node) so each cited hunk line carries an exact file+line+kind+text. Handle `isnew` changesets (whole file added) as a single "new file" cite.
4. Implement `_gather_path_bodies`: for each `CallEdge` function symbol on `candidate.call_path`, call `searchfox.define(symbol, repo=<channel-repo>)`; cap at `max_path_bodies`, truncate each body to `max_body_chars`, and record the permalink + symbol-id. On a `define` miss (virtual/macro/template/cross-language — §7), append a caveat string and continue. Prefer truncating around the changed line range (using `Changeset.*_lines`) over head-truncation so the relevant code survives (see Risks).
5. Implement `_build_prompt`: system message states the role, the grounding rule ("cite source lines and diff lines; if you cannot ground the data flow, return `insufficient`"), and the revision-drift warning (the candidate patch is itself part of what differs between the crashed build and indexed source). User message = stable prefix (`crash_brief` + full candidate diff) followed by the volatile pair-specific block (the one `(patch, frame)` target + gathered bodies) last. Prompt caching of that prefix is SDK/CLI-managed by the Agent SDK/CLI (see #02) — no manual `cache_control`/min-prefix sizing here.
6. Wire `trace_dataflow`: assemble context and hand it to the `data-flow-tracer` `AgentDefinition` (authored in `crashclouseau/agent/roles.py`, `model` from the `agent.llm.roles` block — `sonnet` default, `opus` escalation), which #02's principal loop spawns via the built-in `Task` tool; the role runs the SDK's own multi-turn tool-use loop and ends its final message with a trailing ` ```json ` `DataFlowHypothesis` block. `effort` + adaptive thinking are set once at the `ClaudeAgentOptions` (principal session) level by #02, never on the role's `AgentDefinition` (Sonnet 5 and Opus 4.8 both support options-level `effort` + adaptive thinking; only Haiku 4.5 rejects `effort`). Prompt caching is SDK/CLI-managed (no manual `cache_control`). This unit only supplies the role prompt + tool allowlist + output model; #02 parses the trailing block and #03's `validate_role_fragment` validates it.
7. Post-validate: if `evidence_lines` is empty, or any `Citation` is missing a permalink/symbol-id (for body lines) or a diff anchor (for diff lines), downgrade `verdict` to `insufficient` and log. Attach to `candidate.dataflow`.
8. Add `agent.dataflow` config block + a `config.get_dataflow_cfg()` accessor (or reuse the LLM sub-plan's accessor). `config.py` currently has no `get_agent_*` helper, so one must be added.
9. Tests: a golden case with the SDK (`ClaudeSDKClient`/MCP tools) mocked reproducing the PLAN's example (`Release(mObj)` moved before an early return causes UAF derefed at frame0); a `define`-miss case asserting a caveat + non-crash; a diff-anchoring unit test (mocking `RawRevision.get_revision` and a `Changeset` row); a validation-rejection test (uncited claim -> `insufficient`).
10. Run `flake8`/`black` to match repo style; ensure no module-import side effects (config/`db` not touched at import time, consistent with `worker.py`/`config.py`).

## Risks & open questions
- **Searchfox revision drift (§7).** `define` returns ~tip bodies, not the crash build node's source. The changed function may differ from what crashed. Mitigation: pass drift as an explicit caveat and instruct the model to prefer the *diff* (which is build-accurate via `RawRevision.get_revision`) over the indexed body when they conflict, and to lower confidence when only the indexed body supports a claim.
- **Call-graph holes (§7).** If `call_path` is missing the decisive hop (virtual/IPC/FFI), the data flow is unprovable -> must return `insufficient`, not guess. Confirm Call-graph Explorer marks indirect edges so the Tracer can caveat them.
- **Body size vs. context/cost.** Hot functions can be huge; `max_body_chars` truncation may cut the relevant code. Resolution (step 4): truncate around the changed line range using `Changeset.*_lines` rather than head-truncating.
- **Model tier.** Sonnet 5 vs Opus 4.8 for this role is a Phase-0/Phase-4 tuning question (PLAN §5, §9); keep it pure config. Haiku 4.5 is explicitly too weak here and rejects the `effort` parameter (and has no extended thinking) — do not allow it as the default for this role.
- **`patch.parse` vs raw diff duplication — resolved by `#14`.** The patch-extraction foundation is the single source of hunk text + line cites; `_anchor_diff` reads from it rather than re-fetching `RawRevision`. FLAT line numbers (`patch.parse` / `Changeset.*_lines`) remain the source of the added/deleted/touched kind tag.
- **`Changeset.find` shape.** `find` returns `{filename: [node, ...]}`, not `Changeset` rows — to read the `*_lines` arrays for a candidate node, fetch the changeset row by node (the candidate set / Patch Scout already do this upstream; confirm the row is reachable from `candidate`).
- **searchfox-cli "define" surface unverified here.** The exact subcommand name and flag spelling for full-function source extraction live behind the Call-graph Explorer adapter; this unit must not assume them. If the adapter's `define(symbol, repo)` signature differs, adapt at that boundary only.
- **Multiple paths per candidate.** A candidate may connect to several frames. `path_index` handles one; the orchestrator (worker sub-plan) decides how many paths to trace per candidate within budget.

## Acceptance criteria
- `trace_dataflow(dossier, candidate)` returns a schema-valid `DataFlowHypothesis` for a real dossier; the role's trailing ` ```json ` block validates against `DataFlowHypothesis` via #03's `validate_role_fragment("data-flow-tracer", ...)` (coerced to `verdict="insufficient"` on failure) — the model obeys Claude JSON-schema limits: `additionalProperties:false`, no `minLength`/`maxLength`/`minimum`/`maximum`, no recursion.
- Every `plausible`/`implausible` hypothesis carries at least one `Citation` that is either a diff anchor (file+line+kind from `RawRevision.get_revision`/`Changeset.*_lines`) or a body line (file+symbol-id+permalink from `searchfox.define`); a hypothesis without such citations is automatically `insufficient`. (Grounding rule, PLAN §2/§6.)
- The golden UAF test (`Release(mObj)` moved before early return; frame0 derefs freed `mObj`; matches poison address in `crash_brief`) yields `verdict="plausible"`, `mechanism="uaf"`, `target_value` naming `mObj`, and citations to both the moved diff line and the frame0 deref.
- A `define` miss / missing decisive edge yields `verdict="insufficient"` with a populated `caveats`, and never raises.
- The stable crash-brief + diff prefix is placed first and the per-pair block is last (verifiable in the assembled messages structure), so SDK-managed prompt caching can reuse the prefix across repeated pairs on the same candidate (caching is handled by the Agent SDK/CLI per #02).
- Model id is read from `config.agent.dataflow.model` (defaults `claude-sonnet-5`); no model id string is hardcoded in `dataflow.py`.
- `flake8`/`black` clean; no DB or network access at import time; unit tests pass with the SDK (`ClaudeSDKClient`/MCP tools) and `searchfox.define` mocked (no live API/CLI calls in CI).
