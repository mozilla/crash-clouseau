{{ socorro_comment }}
There {% if count == 1 %}is 1 crash{% else %}are {{ count }} crashes{%endif%} (from {% if installs == 1 %}1 installation{% else %}are {{ installs }} installations{%endif%}) in {{ channel }} {{ version }} {% if not first %}starting {%endif%}with buildid {{ buildid }}. In analyzing the backtrace, the regression may have been introduced by patch [1] to fix bug {{ bugid }}.

[1] {{ changeset_url }}
